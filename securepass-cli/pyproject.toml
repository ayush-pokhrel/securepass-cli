[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "securepass-cli"
version = "0.1.0"
description = "CLI password generator with entropy analysis"
authors = [{ name = "Your Name" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"

[project.scripts]
securepass = "securepass.cli:main"

